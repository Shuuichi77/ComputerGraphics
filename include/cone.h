#ifndef __CONE_H__
#define __CONE_H__

#include <g3x.h>
#include "shape.h"

static double const CONE_H = 2; // Cone height length
static double const CONE_R = 1; // Cone radius length

ShapePtr initCone();

#endif